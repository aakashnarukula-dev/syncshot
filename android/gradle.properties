org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.caching=true
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official
# Truecaller partner key for com.app.screenshotx. Empty until the partner app is
# registered (see README/report). Override with -PTRUECALLER_PARTNER_KEY=... or
# the TRUECALLER_PARTNER_KEY env var; no code change needed.
TRUECALLER_PARTNER_KEY=
